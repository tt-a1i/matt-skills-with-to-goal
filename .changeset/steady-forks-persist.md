---
"matt-skills-with-to-goal": minor
---

Extend `execute-spec-in-fork` with an explicit persistent Goal mode for approved dependency-ordered work. Launches now preserve and validate the native fork receipt, report target-directory alignment, verify startup once, and retain partially created children for explicit recovery instead of creating duplicates. The existing bounded `SPEC READY` path and `spec-executor` contract remain unchanged.
