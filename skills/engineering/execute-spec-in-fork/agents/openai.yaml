interface:
  display_name: "Execute Spec in Fork"
  short_description: "Fork approved work, execute it, and return evidence"
  default_prompt: "Use $execute-spec-in-fork to run the approved work in a Codex fork and return its receipt."
