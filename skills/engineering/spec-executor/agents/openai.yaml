interface:
  display_name: "Spec Executor"
  short_description: "Execute approved work and return an evidence receipt"
  default_prompt: "Use $spec-executor to implement the approved source in this isolated conversation and return a structured execution receipt."
